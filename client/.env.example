VITE_API_URL=http://localhost:5000/api

# Optional: set to true (alongside HTTPS_CERT_PATH/HTTPS_KEY_PATH on the
# server, and certs/cert.pem + certs/key.pem existing) to serve this dev
# server over local HTTPS too. See README.md "Local HTTPS".
# VITE_HTTPS=true
